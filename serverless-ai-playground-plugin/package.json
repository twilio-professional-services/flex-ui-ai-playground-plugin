{
  "name": "serverless-ai-playground-plugin",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "twilio": "5.12.1",
    "@twilio/runtime-handler": "2.0.3",
    "twilio-flex-token-validator": "^1.6.0"
  },
  "devDependencies": {
    "twilio-run": "^4.2.0"
  },
  "engines": {
    "node": "22"
  }
}